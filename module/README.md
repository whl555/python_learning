1. 主模块与副模块
- 主模块, 没有被import的module
- 副模块, 被import的module

2. __main__
if __name__ == '__main__'中的逻辑, 主模块调用, 副模块不调用

3. ' __init__.py'的作用
- 模块配置: 例如模块环境变量、端口等配置信息
- 版本信息: 包括作者、版本号等
- 导出信息: export导出给外部使用的接口

4. 绝对导入与相对导入
    1. 相对导入是建议在 package 内部之间的 module 相互使用的。
    2. 入口文件里使用 package 一般使用绝对路径导入。
